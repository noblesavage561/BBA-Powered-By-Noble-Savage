"""
Backend unit & integration tests for BBA Services OS.

Tests run against the FastAPI app directly (no Docker required).
Database and Redis connections are gracefully skipped when unavailable.
"""
